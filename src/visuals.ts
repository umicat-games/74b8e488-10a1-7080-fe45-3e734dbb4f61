import type { RenderScriptModule } from '@umicat/phaser-sdk';

// This game draws everything with the Phaser Graphics API — no code-rendered entities.
export const renderScripts: Record<string, RenderScriptModule> = {};
